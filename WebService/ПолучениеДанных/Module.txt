
Функция ПолучениеПриходныхНакладных(ДатаН, ДатаК, ТипДок)
	Запрос=Новый Запрос;
	Запрос.Текст="ВЫБРАТЬ
	             |	ПриходнаяНакладная.Номер,
	             |	НАЧАЛОПЕРИОДА(ПриходнаяНакладная.Дата, ДЕНЬ) КАК Дата,
	             |	ПриходнаяНакладная.Контрагент.Наименование,
				 |	ПриходнаяНакладная.Контрагент.Код,
	             |	ПриходнаяНакладная.СуммаИтого,
	             |	ПриходнаяНакладная.СуммаНДСИтого
	             |ИЗ
	             |	Документ."+?(ТипДок=1,"ПриходнаяНакладная","КорректировкаПоступления")+" КАК ПриходнаяНакладная
	             |ГДЕ
	             |	НЕ ПриходнаяНакладная.ПометкаУдаления
	             //|	И ПриходнаяНакладная.Проведен
	             |	И ПриходнаяНакладная.Дата МЕЖДУ &ДатаН И &ДатаК";
				 
	Запрос.Параметры.Вставить("ДатаН",ДатаН);
	Запрос.Параметры.Вставить("ДатаК",ДатаК);
		
	Резт=Запрос.Выполнить().Выгрузить();
	
	ТаблицаОтчетаТип=ФабрикаXDTO.Тип("http://www.sample-package.org","Таблица");
	СтрокаТаблицыОтчетаТип=ФабрикаXDTO.Тип("http://www.sample-package.org","СтрокаТаблицы");
	
	ТаблицаОтчета=ФабрикаXDTO.Создать(ТаблицаОтчетаТип);
	
	Для Каждого СтрРезт Из Резт Цикл
		СтрокаТаблицыОтчета=ФабрикаXDTO.Создать(СтрокаТаблицыОтчетаТип);
		
		СтрокаТаблицыОтчета.Заполнить(СтрРезт);
		
		ТаблицаОтчета.Состав.Добавить(СтрокаТаблицыОтчета);
		
	КонецЦикла;
	
	Возврат ТаблицаОтчета;
КонецФункции
