
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ИмяМакета = "";
	ЭтаФорма.Параметры.Свойство("ИмяМакета",ИмяМакета);
	Если Не ЗначениеЗаполнено(ИмяМакета) тогда
		Отказ = Истина;
	КонецЕсли;
	
	СхемаКомпоновкиДанных = Отчеты.СверкаСRMS.ПолучитьМакет(ИмяМакета);
	//следующий цикл необходим для отображения на форме параметров
	Для Каждого ПараметрКомпоновщика ИЗ СхемаКомпоновкиДанных.Параметры Цикл
		ПараметрКомпоновщика.ВключатьВДоступныеПоля   = Ложь;
		ПараметрКомпоновщика.ОграничениеИспользования = Ложь;
	КонецЦикла;
	Отчет.КомпоновщикНастроек.Инициализировать(Новый ИсточникДоступныхНастроекКомпоновкиДанных(ПоместитьВоВременноеХранилище(СхемаКомпоновкиДанных, УникальныйИдентификатор)));
	
КонецПроцедуры
