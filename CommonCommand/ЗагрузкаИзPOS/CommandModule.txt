
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	Состояние("Загружается...");
	НаСервере();
	Состояние("Загружено"); 
КонецПроцедуры

&НаСервере
Процедура НаСервере()
	Izh_ОбменДаннымиPOS.Загрузить();
КонецПроцедуры