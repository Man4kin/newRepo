// {ВетровИА 2015-08-11 Обращение 0000135383
&НаСервереБезКонтекста
Функция ПолучитьСписокВыбора()
	
	СписокВыбора = Новый СписокЗначений;
    СписокВыбора.Добавить(Перечисления.РезультатПриемки.Брак);
    СписокВыбора.Добавить(Перечисления.РезультатПриемки.Недогруз);
	
	Возврат СписокВыбора; 
КонецФункции	

&НаКлиенте
Процедура ПризнакПриемкиНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	СписокВыбора = ПолучитьСписокВыбора();
    ВыбранныйЭлемент = ВыбратьИзСписка(СписокВыбора, Элемент, СписокВыбора.НайтиПоЗначению(Объект.ПризнакПриемки));
    
    Если ВыбранныйЭлемент <> Неопределено Тогда
        Объект.ПризнакПриемки = ВыбранныйЭлемент.Значение;
    КонецЕсли;
    
КонецПроцедуры
// ВетровИА 2015-08-11 Обращение 0000135383}
