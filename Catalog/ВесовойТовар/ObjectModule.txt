
Процедура ПередЗаписью(Отказ)
	
	Если Ссылка.Пустая() Тогда
		Код = ШтриховыеКоды.ПолучитьНовыйPLU(Владелец);
	КонецЕсли;
	
	Если Ссылка.Номенклатура <> Номенклатура Тогда
		//Если Не Номенклатура.ТипТовара = Перечисления.ТипыТоваров.Весовой Тогда
		//	Сообщить("Товар " + СокрЛП(Номенклатура) + " - невесовой!");
		//	Отказ = Истина;
		//КонецЕсли;
		Если Не Отказ Тогда
			Отказ = ШтриховыеКоды.ПроверкаВыбораВесовогоТовара(Владелец, Номенклатура);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры
