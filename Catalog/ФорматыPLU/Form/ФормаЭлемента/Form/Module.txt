&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Объект.Ссылка.Пустая() Тогда
		Объект.Наименование = "-";
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	СоставитьПредставление();
	
КонецПроцедуры

&НаКлиенте
Процедура ДлинаПрефиксаПриИзменении(Элемент)
	СоставитьПредставление();
КонецПроцедуры

&НаКлиенте
Процедура ДлинаКодаПриИзменении(Элемент)
	СоставитьПредставление();
КонецПроцедуры

&НаКлиенте
Процедура ДлинаКоличестваПриИзменении(Элемент)
	СоставитьПредставление();
КонецПроцедуры

&НаКлиенте
Процедура СоставитьПредставление()
	
	ПредставлениеФормата = "";
	Для Сч = 1 По Объект.ДлинаПрефикса Цикл
		ПредставлениеФормата = ПредставлениеФормата + "П";
	КонецЦикла;
	Для Сч = 1 По Объект.ДлинаКода Цикл
		ПредставлениеФормата = ПредставлениеФормата + "Т";
	КонецЦикла;
	Для Сч = 1 По Объект.ДлинаКоличества Цикл
		ПредставлениеФормата = ПредставлениеФормата + "К";
	КонецЦикла;
	
КонецПроцедуры
