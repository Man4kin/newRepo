Функция ФормаВыполненияЗадачи(Знач ЗадачаСсылка, Знач ТочкаМаршрутаСсылка) Экспорт
	Результат = Новый Структура;
	Если ТочкаМаршрутаСсылка = БизнесПроцессы.НоваяЦена.ТочкиМаршрута.ПечатьЦенников тогда
		Запрос = Новый Запрос;
		Запрос.Текст = "ВЫБРАТЬ
		|	НоваяЦенаСостав.Номенклатура,
		|	НоваяЦенаСостав.ЕдиницаИзмерения,
		|	НоваяЦенаСостав.Цена,
		|	НоваяЦенаСостав.ID,
		|	НоваяЦенаСостав.ШаблонЦенника,
		|	&Фирма как Фирма
		|ИЗ
		|	БизнесПроцесс.НоваяЦена.Состав КАК НоваяЦенаСостав
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ (ВЫБРАТЬ
		|			НоваяЦенаСостав.Номенклатура КАК Номенклатура,
		|			НоваяЦенаСостав.ЕдиницаИзмерения КАК ЕдиницаИзмерения,
		|			МАКСИМУМ(НоваяЦенаСостав.ID) КАК ID
		|		ИЗ
		|			БизнесПроцесс.НоваяЦена.Состав КАК НоваяЦенаСостав
		|		ГДЕ
		|			НоваяЦенаСостав.Ссылка = &Ссылка
		|		
		|		СГРУППИРОВАТЬ ПО
		|			НоваяЦенаСостав.Номенклатура,
		|			НоваяЦенаСостав.ЕдиницаИзмерения) КАК ВложенныйЗапрос
		|		ПО НоваяЦенаСостав.Номенклатура = ВложенныйЗапрос.Номенклатура
		|			И НоваяЦенаСостав.ЕдиницаИзмерения = ВложенныйЗапрос.ЕдиницаИзмерения
		|			И НоваяЦенаСостав.ID = ВложенныйЗапрос.ID
		|ГДЕ
		|	НоваяЦенаСостав.Ссылка = &Ссылка";
		Запрос.УстановитьПараметр("Ссылка", ЗадачаСсылка.БизнесПроцесс);
		Фирма = ПараметрыСеанса.ТекущаяСтруктурнаяЕдиница.Организация;
		Запрос.УстановитьПараметр("Фирма", Фирма);
		СтруктураПараметров = Новый Структура("Состав, ДатаАкции, Фирма", Запрос.Выполнить().Выгрузить(),ЗадачаСсылка.БизнесПроцесс.Основание,Фирма);
		//СтруктураПараметров = Новый Структура("Состав",ЗадачаСсылка.БизнесПроцесс.Состав.Выгрузить(,"Номенклатура,ЕдиницаИзмерения,Цена"));
		АдресВхранилище = ПоместитьВоВременноеХранилище(СтруктураПараметров);	 
		Результат.Вставить("ПараметрыФормы", Новый Структура("ВыполняемаяЗадача,Режим,АдресВхранилище",ЗадачаСсылка,"ПечатьЦенников",АдресВхранилище));
		ИмяФормы = "Обработка.ПечатьЦенниковИЭтикеток.Форма.ПечатьЦенниковИЭтикеток";
		Результат.Вставить("ИмяФормы", ИмяФормы);
	Иначе	
		Результат.Вставить("Выполнять",Истина);
	КонецЕсли;
	Возврат Результат;
КонецФункции // ФормаВыполненияЗадачи()

