
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	ЗадачаСсылка = Параметры.Ключ;
	ЗначениеВДанныеФормы(ЗадачаСсылка.ПолучитьОбъект(),ЗадачаОбъект);
	БизнесПроцесс = ЗадачаСсылка.БизнесПроцесс;
	Если Параметры.ТочкаМаршрута = БизнесПроцессы.НоваяРаспродажа.ТочкиМаршрута.ОповеститьОНачале Тогда
		Состав.Загрузить(БизнесПроцесс.Состав.Выгрузить());
	Иначе
		Для Каждого Строка Из БизнесПроцесс.Состав Цикл
			НоваяСтрока = Состав.Добавить();
			ЗаполнитьЗначенияСвойств(НоваяСтрока,Строка);
			НоваяСтрока.Цена = УправлениеЦенамиСервер.ПолучитьЦенуПоID(Строка.IDВосстановления,"ID");
		КонецЦикла;
	КонецЕсли;	
КонецПроцедуры
