//dps+ 15/05/18 наряд 149977

Функция ФормаВыполненияЗадачи(Знач Задача, Знач ТочкаМаршрута) Экспорт 

	//Результат = Новый Структура;
	//Результат.Вставить("ПараметрыФормы", Новый Структура("Ключ,ТочкаМаршрута", ЗадачаСсылка,ТочкаМаршрутаСсылка));
	//Результат.Вставить("ИмяФормы", "БизнесПроцесс.АвтоЗаказ.Форма.УтверждениеПотребности");
	//Возврат Результат;

	СписокВариантов = Новый Соответствие();
	//++БИТ БВО - задача по УП
	//ДобавитьВариантОткрытияФормы(СписокВариантов, ТочкиМаршрута.УтверждениеПотребности, "БизнесПроцесс.АвтоЗаказ.Форма.УтверждениеПотребности", Задача);
	ДобавитьВариантОткрытияФормы(СписокВариантов, ТочкиМаршрута.УтверждениеПотребности, "БизнесПроцесс.АвтоЗаказ.Форма.УтверждениеПотребности2", Задача);
	//--БИТ БВО - задача по УП
	ДобавитьВариантОткрытияФормы(СписокВариантов, ТочкиМаршрута.УтверждениеПотребностиBI, "БизнесПроцесс.АвтоЗаказ.Форма.УтверждениеПотребностиBI", Задача);
	Результат = СписокВариантов[ТочкаМаршрута];
	Если Результат = Неопределено Тогда
		Результат = Новый Структура();
	КонецЕсли;
	Возврат Результат;
КонецФункции

Процедура ДобавитьВариантОткрытияФормы(СписокВариантов, Знач ТочкаМаршрута, Знач ИмяФормы, Знач ПараметрыФормы)
	Если ТипЗнч(ПараметрыФормы) <> Тип("Структура") Тогда
		ПараметрыФормы = Новый Структура("Ключ", ПараметрыФормы);
	КонецЕсли;
	СписокВариантов.Вставить(ТочкаМаршрута, Новый Структура("ИмяФормы, ПараметрыФормы",	ИмяФормы, ПараметрыФормы));
КонецПроцедуры
//dps- 15/05/18 наряд 149977
