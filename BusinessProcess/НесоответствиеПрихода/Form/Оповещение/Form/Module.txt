
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	ЗадачаСсылка = Параметры.Ключ;
	ЗначениеВДанныеФормы(ЗадачаСсылка.ПолучитьОбъект(),ЗадачаОбъект);
	ЗначениеВДанныеФормы(ЗадачаСсылка.БизнесПроцесс.ПолучитьОбъект(),БизнесПроцесс);
	Элементы.Страницы.ПодчиненныеЭлементы[ЗадачаСсылка.ТочкаМаршрута.Имя].Видимость = Истина;
	Если НЕ ЗначениеЗаполнено(ЗадачаСсылка.БизнесПроцесс.Основание.ДокОснование) Тогда
		Элементы.БизнесПроцессНесоответствиеПоЦенам.ПодчиненныеЭлементы.БизнесПроцессНесоответствиеПоЦенамЦенаЗаказа.Заголовок = "Цена контрагента";
	КонецЕсли;	
КонецПроцедуры

&НаКлиенте
Процедура Печать(Команда)
	// Вставить содержимое обработчика.
КонецПроцедуры
