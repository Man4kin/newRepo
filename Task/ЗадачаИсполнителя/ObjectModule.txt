
Процедура ПередЗаписью(Отказ)
	Если ЗначениеЗаполнено(БизнесПроцесс) И ПометкаУдаления И ТипЗнч(БизнесПроцесс) <> Тип("БизнесПроцессСсылка.АвтоЗаказ") Тогда
		Отказ = Истина;
		Возврат;
	КонецЕсли;	
	Если Выполнена Тогда
		ЗадачуВыполнил = ПараметрыСеанса.ТекущийПользователь;
		ДатаВыполненияФакт = ТекущаяДата();
	КонецЕсли;
	//Если Напоминать = Ложь Тогда
	//	ДатаНапоминания = "";
	//КонецЕсли;
КонецПроцедуры

Процедура ОбработкаЗаполнения(ДанныеЗаполнения, СтандартнаяОбработка)
	ЗаполнитьПоУмолчанию();
КонецПроцедуры

Процедура ЗаполнитьПоУмолчанию() экспорт
	Дата = ТекущаяДата(); // это время создания
	//СтруктурнаяЕдиница = ПараметрыСеанса.ТекущаяСтруктурнаяЕдиница; // для текущей структурной единицы
	//Если БизнесПроцесс = Неопределено Тогда
		Автор = ПараметрыСеанса.ТекущийПользователь; // создал текущий сотрудник
	//КонецЕсли;	
	РазрешитьИнтерактивноеВыполнение = Ложь; // нельзя выполнять "руками"
	Повторяющаяся = Ложь; // задача не является повторяющейся
КонецПроцедуры

