&НаКлиенте
Процедура СписокДатПроизводстваТовараВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	СтандартнаяОбработка = Ложь;
	ТекСтрока = Элемент.ДанныеСтроки(ВыбраннаяСтрока);
	Структура = Новый Структура;
	Структура.Вставить("ДокументПриход", ТекСтрока.Документ);
	Структура.Вставить("ДатаПроизводства", ТекСтрока.ДатаПроизводства);	
	ОповеститьОВыборе(Структура); 
КонецПроцедуры

&НаКлиенте
Процедура СписокДатПроизводстваТовараВыборЗначения(Элемент, Значение, СтандартнаяОбработка)
	СтандартнаяОбработка = Ложь;
	ТекСтрока = Элемент.ДанныеСтроки(Значение);
	Структура = Новый Структура;
	Структура.Вставить("ДокументПриход", ТекСтрока.Документ);
	Структура.Вставить("ДатаПроизводства", ТекСтрока.ДатаПроизводства);	
	ОповеститьОВыборе(Структура);
КонецПроцедуры
