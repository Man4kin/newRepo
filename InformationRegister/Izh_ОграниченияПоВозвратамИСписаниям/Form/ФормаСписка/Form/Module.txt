
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если Не ПараметрыСеанса.ЭтоЦентр Тогда 
		Параметры.Отбор.Вставить("СтруктурнаяЕдиница",ПараметрыСеанса.ТекущаяСтруктурнаяЕдиница);
	КонецЕсли;
	Если Не РольДоступна("Ревизор") и Не РольДоступна("ПолныеПрава") Тогда 
		ЭтаФорма.Доступность=Ложь;
	КонецЕсли;
КонецПроцедуры
