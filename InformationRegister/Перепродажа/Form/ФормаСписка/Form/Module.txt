
&НаКлиенте
Процедура СрезПоследних(Команда)
	Элементы.ФормаСрезПоследних.Пометка = Не Элементы.ФормаСрезПоследних.Пометка;
	ЗаменитьОсновнуюТаблицу(Элементы.ФормаСрезПоследних.Пометка);
КонецПроцедуры

&НаСервере
Процедура ЗаменитьОсновнуюТаблицу(Пометка)
	Если Пометка Тогда
		Список.ОсновнаяТаблица = "РегистрСведений.Перепродажа.СрезПоследних";
	Иначе
		Список.ОсновнаяТаблица = "РегистрСведений.Перепродажа";
	КонецЕсли;	
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если НЕ ПараметрыСеанса.ЭтоЦентр Тогда
		ТолькоПросмотр = Истина;
	КонецЕсли;	
КонецПроцедуры
