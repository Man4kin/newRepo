

Процедура ПередЗаписью(Отказ, Замещение)
	//Если Не Отказ И ЭтотОбъект.Количество()>0 Тогда
	//	Если ЭтотОбъект[0].АктивностьЗаписи Тогда
	//		Выборка = ПланыОбмена.ОбменSMS.Выбрать();
	//		Пока Выборка.Следующий() Цикл
	//			Если ПланыОбмена.ОбменSMS.ЭтотУзел() = Выборка.Ссылка Тогда
	//				Продолжить;
	//			КонецЕсли;	
	//			ОбменДанными.Получатели.Добавить(Выборка.Ссылка);
	//		КонецЦикла;	
	//	КонецЕсли;	
	//Конецесли;
КонецПроцедуры

