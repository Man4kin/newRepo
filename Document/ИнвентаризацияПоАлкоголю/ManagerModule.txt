Функция ПечатьИНВ19(МассивОбъектов,ОбъектыПечати) экспорт
	Макет = ПолучитьМакет("ИНВ19");
	ТабДок= новый ТабличныйДокумент;
	ТабДок.ИмяПараметровПечати = "ПАРАМЕТРЫ_ПЕЧАТИ_Инвентаризация_ИНВ19";
	НомерСтрокиНачало = 0;
	для каждого док из МАссивОбъектов цикл
		ЗапросФИрмаСклад = новый Запрос;
		ЗапросФирмаСклад.Текст = "ВЫБРАТЬ РАЗЛИЧНЫЕ
		                         |	ИнвентаризацияСоставПоФирмам.Фирма,
		                         |	ИнвентаризацияСоставПоФирмам.Склад
		                         |ИЗ
		                         |	Документ.Инвентаризация.СоставПоФирмам КАК ИнвентаризацияСоставПоФирмам
		                         |ГДЕ
		                         |	ИнвентаризацияСоставПоФирмам.Ссылка = &Ссылка";
		ЗапросФирмаСклад.УстановитьПараметр("ссылка",док.ссылка);
		Рез = ЗапросФирмаСклад.Выполнить();
		ВыборкаФирмаСклад=Рез.Выбрать();
		Пока ВыборкаФирмаСклад.Следующий() цикл
		Запрос = новый Запрос;
		Запрос.УстановитьПараметр("Ссылка",Док.Ссылка);
		Запрос.Текст = "ВЫБРАТЬ
		               |	ВложенныйЗапрос.Номенклатура,
		               |	ВложенныйЗапрос.Номенклатура.Наименование КАК ТоварНаименование,
		               |	ВложенныйЗапрос.Номенклатура.Код КАК ТоварКод,
		               |	ВложенныйЗапрос.Номенклатура.БазоваяЕдиницаИзмерения КАК ЕдиницаИзмеренияНаименование,
		               |	ВЫБОР
		               |		КОГДА ВложенныйЗапрос.Сумма > 0
		               |			ТОГДА ВложенныйЗапрос.Сумма
		               |		ИНАЧЕ 0
		               |	КОНЕЦ КАК СуммаИзлишек,
		               |	ВЫБОР
		               |		КОГДА ВложенныйЗапрос.Сумма < 0
		               |			ТОГДА -1 * ВложенныйЗапрос.Сумма
		               |		ИНАЧЕ 0
		               |	КОНЕЦ КАК СуммаНедостача,
		               |	ВЫБОР
		               |		КОГДА ВложенныйЗапрос.СуммаПересорт > 0
		               |			ТОГДА ВложенныйЗапрос.СуммаПересорт
		               |		ИНАЧЕ 0
		               |	КОНЕЦ КАК СуммаПересортИзлишек,
		               |	ВЫБОР
		               |		КОГДА ВложенныйЗапрос.СуммаПересорт < 0
		               |			ТОГДА -1 * ВложенныйЗапрос.СуммаПересорт
		               |		ИНАЧЕ 0
		               |	КОНЕЦ КАК СуммаПересортНедостача,
		               |	ВЫБОР
		               |		КОГДА ВложенныйЗапрос.Количество > 0
		               |			ТОГДА ВложенныйЗапрос.Количество
		               |		ИНАЧЕ 0
		               |	КОНЕЦ КАК КоличествоИзлишек,
		               |	ВЫБОР
		               |		КОГДА ВложенныйЗапрос.Количество < 0
		               |			ТОГДА -1 * ВложенныйЗапрос.Количество
		               |		ИНАЧЕ 0
		               |	КОНЕЦ КАК КоличествоНедостача,
		               |	ВЫБОР
		               |		КОГДА ВложенныйЗапрос.КоличествоПересорт > 0
		               |			ТОГДА ВложенныйЗапрос.КоличествоПересорт
		               |		ИНАЧЕ 0
		               |	КОНЕЦ КАК КоличествоПересортИзлишек,
		               |	ВЫБОР
		               |		КОГДА ВложенныйЗапрос.КоличествоПересорт < 0
		               |			ТОГДА -1 * ВложенныйЗапрос.КоличествоПересорт
		               |		ИНАЧЕ 0
		               |	КОНЕЦ КАК КоличествоПересортНедостача,
		               |	ВЫБОР
		               |		КОГДА ВложенныйЗапрос.КоличествоПересорт > 0
		               |			ТОГДА ВложенныйЗапрос.НомерСтрокиПересорт
		               |	КОНЕЦ КАК НомерСтрокиПересортИзлишки,
		               |	ВЫБОР
		               |		КОГДА ВложенныйЗапрос.КоличествоПересорт < 0
		               |			ТОГДА ВложенныйЗапрос.НомерСтрокиПересорт
		               |	КОНЕЦ КАК НомерСтрокиПересортНедостачи,
		               |	ВложенныйЗапрос.Фирма,
		               |	ВложенныйЗапрос.Склад.Владелец как СтруктурнаяЕдиница, //+Зернятко А.В.@06.11.2013 - //AKB-374. Ответственные лица СЕ
		               |	ВложенныйЗапрос.Склад
		               |ИЗ
		               |	(ВЫБРАТЬ
		               |		СведенияПоИнвентаризации.Номенклатура КАК Номенклатура,
		               |		СведенияПоИнвентаризации.Сумма - СведенияПоИнвентаризации.СуммаНДС КАК Сумма,
		               |		СведенияПоИнвентаризации.СуммаПересорт - СведенияПоИнвентаризации.СуммаНДСПересорт КАК СуммаПересорт,
		               |		СведенияПоИнвентаризации.Количество КАК Количество,
		               |		СведенияПоИнвентаризации.КоличествоПересорт КАК КоличествоПересорт,
		               |		СведенияПоИнвентаризации.НомерСтрокиПересорт КАК НомерСтрокиПересорт,
		               |		СведенияПоИнвентаризации.Склад КАК Склад,
		               |		СведенияПоИнвентаризации.Фирма КАК Фирма
		               |	ИЗ
		               |		РегистрСведений.СведенияПоИнвентаризации КАК СведенияПоИнвентаризации
		               |	ГДЕ
		               |		СведенияПоИнвентаризации.Регистратор = &ССылка
		               |		И СведенияПоИнвентаризации.Склад = &Склад
		               |		И СведенияПоИнвентаризации.Фирма = &Фирма) КАК ВложенныйЗапрос";
					   Запрос.УстановитьПараметр("Склад",ВыборкаФирмаСклад.склад);
					   Запрос.УстановитьПараметр("ФИрма",ВыборкаФирмаСклад.Фирма);
		Рез = Запрос.Выполнить();
		Область = Макет.ПолучитьОбласть("Шапка");
		СведенияОбОрганизации = ОбработкаПечатиСервер.СведенияОЮрФизЛице(ВыборкаФирмаСклад.Фирма,Док.Дата,Док.СтруктурнаяЕдиница);
		Область.Параметры.ПредставлениеОрганизации = ОбработкаПечатиСервер.ОписаниеОрганизации(ОбработкаПечатиСервер.СведенияОЮрФизЛице(ВыборкаФирмаСклад.Фирма, Док.Дата, Док.СтруктурнаяЕдиница));
		Область.Параметры.ОрганизацияПоОКПО = СведенияОбОрганизации.КодПоОКПО;
		Область.Параметры.ПредставлениеПодразделения = Док.СтруктурнаяЕдиница.Наименование;			
		Область.Параметры.Основание = Док.Основание;
		Область.Параметры.ОрганизацияПоОКПО        = СведенияОбОрганизации.КодПоОКПО;
		Область.Параметры.ДатаДокумента           = Док.Дата;
		Область.Параметры.НомерДокумента           = Док.Номер;
		СтрокНаСтранице = 19;
		НомерСтраницы   = 2;
		Ном             = 0;  
		КолвоСтрокПоСтранице = 0;
		ТабДок.вывести(Область);
		ТабДок.ВывестиГоризонтальныйРазделительСтраниц();
		ЗаголовокТаблицы = Макет.ПолучитьОбласть("ЗаголовокТаблицы1");
		ЗаголовокТаблицы.Параметры.НомерСтраницы = "Страница " + НомерСтраницы; 
		ТабДок.вывести(ЗаголовокТаблицы);
		Выборка =   Рез.Выбрать();
		ИтогоСтраницыРезультатИзлишекКолво   = 0;
		ИтогоСтраницыРезультатИзлишекСумма   = 0;
		ИтогоСтраницыРезультатНедостачаКолво = 0;
		ИтогоСтраницыРезультатНедостачаСумма = 0;
		ИтогоСтраницыПересортИзлишкиКолво = 0;
		ИтогоСтраницыПересортИзлишкиСумма = 0;
		ИтогоСтраницыПересортНедостачиКолво = 0;
		ИтогоСтраницыПересортНедостачиСумма = 0;
		ИтогоРезультатИзлишекКолво   = 0;
		ИтогоРезультатИзлишекСумма   = 0;
		ИтогоРезультатНедостачаКолво = 0;
		ИтогоРезультатНедостачаСумма = 0;
		ИтогоПересортИзлишкиКолво = 0;
		ИтогоПересортИзлишкиСумма = 0;
		ИтогоПересортНедостачиКолво = 0;
		ИтогоПересортНедостачиСумма = 0;
		
		Пока Выборка.Следующий() цикл
			Если Ном = СтрокНаСтранице Тогда
				Ном = 0;
				ОбластьИтоговПоСтранице = Макет.ПолучитьОбласть("ИтогоСтраницы");
				ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыРезультатИзлишекКолво   = ИтогоСтраницыРезультатИзлишекКолво;
				ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыРезультатИзлишекСумма   = ИтогоСтраницыРезультатИзлишекСумма;
				ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыРезультатНедостачаКолво = ИтогоСтраницыРезультатНедостачаКолво;
				ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыРезультатНедостачаСумма = ИтогоСтраницыРезультатНедостачаСумма;
				ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыПересортИзлишкиКолво = ИтогоСтраницыПересортИзлишкиКолво;
				ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыПересортИзлишкиСумма = ИтогоСтраницыПересортИзлишкиСумма;
				ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыПересортНедостачиКолво = ИтогоСтраницыПересортНедостачиКолво;
				ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыПересортНедостачиСумма = ИтогоСтраницыПересортНедостачиСумма;
				ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыПриходИзлишковКолво = ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыРезультатИзлишекКолво - ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыПересортИзлишкиКолво;
				ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыПриходИзлишковСумма = ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыРезультатИзлишекСумма - ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыПересортИзлишкиСумма;
				ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыСписаниеНедостачКолонка1Колво = ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыРезультатНедостачаКолво - ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыПересортНедостачиКолво;
				ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыСписаниеНедостачКолонка1Сумма = ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыРезультатНедостачаСумма - ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыПересортНедостачиСумма;				
				ТабДок.Вывести(ОбластьИтоговПоСтранице);
				
				ИтогоСтраницыРезультатИзлишекКолво   = 0;
				ИтогоСтраницыРезультатИзлишекСумма   = 0;
				ИтогоСтраницыРезультатНедостачаКолво = 0;
				ИтогоСтраницыРезультатНедостачаСумма = 0;
				ИтогоСтраницыПересортИзлишкиКолво = 0;
				ИтогоСтраницыПересортИзлишкиСумма = 0;
				ИтогоСтраницыПересортНедостачиКолво = 0;
				ИтогоСтраницыПересортНедостачиСумма = 0;
				
				НомерСтраницы = НомерСтраницы + 1;
				ТабДок.ВывестиГоризонтальныйРазделительСтраниц();
				
				ЗаголовокТаблицы.Параметры.НомерСтраницы = "Страница " + НомерСтраницы;
				ТабДок.Вывести(ЗаголовокТаблицы);
			КонецЕсли;
			
			ИтогоРезультатНедостачаКолво = ИтогоРезультатНедостачаКолво + Выборка.количествоНедостача;
			ИтогоРезультатНедостачаСумма =ИтогоРезультатНедостачаСумма + выборка.суммаНедостача;
			ИтогоРезультатИзлишекКолво = ИтогоРезультатИзлишекКолво + Выборка.количествоИзлишек;
			ИтогоРезультатИзлишекСумма = ИтогоРезультатИзлишекСумма + Выборка.суммаИзлишек;			
			ИтогоПересортИзлишкиКолво = ИтогоПересортИзлишкиКолво + Выборка.количествоПересортИзлишек;
			ИтогоПересортИзлишкиСумма = ИтогоПересортИзлишкиСумма + Выборка.СуммаПересортИзлишек;			
			ИтогоПересортНедостачиКолво = ИтогоПересортНедостачиКолво + Выборка.количествоПересортНедостача;
			ИтогоПересортНЕдостачиСумма = ИтогоПересортНедостачиСумма + Выборка.суммаПересортНедостача;			
			
			ИтогоСтраницыРезультатИзлишекКолво   = ИтогоСтраницыРезультатИзлишекКолво + Выборка.КоличествоИзлишек;
			ИтогоСтраницыРезультатИзлишекСумма   = ИтогоСтраницыРезультатИзлишекСумма + Выборка.СуммаИзлишек;
			ИтогоСтраницыРезультатНедостачаКолво = ИтогоСтраницыРезультатНедостачаКолво + Выборка.КоличествоНедостача;
			ИтогоСтраницыРезультатНедостачаСумма = ИтогоСтраницыРезультатНедостачаСумма + Выборка.СуммаНедостача;
			ИтогоСтраницыПересортИзлишкиКолво = ИтогоСтраницыПересортИзлишкиКолво + Выборка.КоличествоПересортИзлишек;
			ИтогоСтраницыПересортИзлишкиСумма = ИтогоСтраницыПересортИзлишкиСумма + Выборка.СуммаПересортИзлишек;
			ИтогоСтраницыПересортНедостачиКолво = ИтогоСтраницыПересортНедостачиколво + Выборка.КоличествоПересортНедостача;
			ИтогоСтраницыПересортНедостачиСумма = ИтогоСтраницыПересортНедостачиСумма + Выборка.СуммаПересортНедостача;
			
			Область = Макет.ПолучитьОбласть("СтрокаТаблицы1");
			ном = ном+1;
			Область.Параметры.номер = ном;;
			ЗаполнитьЗначенияСвойств(Область.Параметры,Выборка);
			Область.Параметры.РезультатИзлишекКолво = Выборка.КоличествоИзлишек - Выборка.КОличествоПересортИЗлишек;
			Область.Параметры.РезультатИзлишекСумма= Выборка.СуммаИзлишек - Выборка.суммаПересортИзлишек ;
			Область.Параметры.РезультатНедостачаКолво  =Выборка.КоличествоНедостача-ВЫборка.КоличествоПересортНедостача;
			Область.Параметры.РезультатНедостачаСумма=   Выборка.СуммаНедостача - ВЫборка.СуммаПересортНедостача;
			Область.Параметры.ПриходИзлишковКолво = ВЫборка.КоличествоИзлишек - Выборка.КоличествоПересортИзлишек;
			Область.Параметры.ПриходИзлишковСумма = Выборка.СуммаИзлишек - Выборка.суммаПересортИзлишек;
			Область.Параметры.СписаниеНедостачКолонка1Колво = Выборка.КоличествоНедостача-ВЫборка.КоличествоПересортНедостача;
			Область.Параметры.СписаниеНедостачКолонка1Сумма = Выборка.СуммаНедостача - ВЫборка.СуммаПересортНедостача;
			ТабДок.вывести(область);
		конеццикла;
		
		
		ОбластьИтоговПоСтранице = Макет.ПолучитьОбласть("ИтогоСтраницы");
		ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыРезультатИзлишекКолво   = ИтогоСтраницыРезультатИзлишекКолво;
		ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыРезультатИзлишекСумма   = ИтогоСтраницыРезультатИзлишекСумма;
		ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыРезультатНедостачаКолво = ИтогоСтраницыРезультатНедостачаКолво;
		ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыРезультатНедостачаСумма = ИтогоСтраницыРезультатНедостачаСумма;
		ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыПересортИзлишкиКолво = ИтогоСтраницыПересортИзлишкиКолво;
		ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыПересортИзлишкиСумма = ИтогоСтраницыПересортИзлишкиСумма;
		ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыПересортНедостачиКолво = ИтогоСтраницыПересортНедостачиКолво;
		ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыПересортНедостачиСумма = ИтогоСтраницыПересортНедостачиСумма;
		ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыПриходИзлишковКолво = ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыРезультатИзлишекКолво - ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыПересортИзлишкиКолво;
		ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыПриходИзлишковСумма = ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыРезультатИзлишекСумма - ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыПересортИзлишкиСумма;
		ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыСписаниеНедостачКолонка1Колво = ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыРезультатНедостачаКолво - ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыПересортНедостачиКолво;
		ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыСписаниеНедостачКолонка1Сумма = ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыРезультатНедостачаСумма - ОбластьИтоговПоСтранице.Параметры.ИтогоСтраницыПересортНедостачиСумма;		
		ТабДок.Вывести(ОбластьИтоговПоСтранице);					
		
		ОбластьИтогов = Макет.ПолучитьОбласть("ИтогоТаблицы1");
		ОбластьИтогов.Параметры.ИтогоРезультатИзлишекКолво   = ИтогоРезультатИзлишекКолво;
		ОбластьИтогов.Параметры.ИтогоРезультатИзлишекСумма   = ИтогоРезультатИзлишекСумма;
		ОбластьИтогов.Параметры.ИтогоРезультатНедостачаКолво = ИтогоРезультатНедостачаКолво;
		ОбластьИтогов.Параметры.ИтогоРезультатНедостачаСумма = ИтогоРезультатНедостачаСумма;
		ОбластьИтогов.Параметры.ИтогоПересортИзлишкиКолво = ИтогоПересортИзлишкиКолво;
		ОбластьИтогов.Параметры.ИтогоПересортИзлишкиСумма = ИтогоПересортИзлишкиСумма;
		ОбластьИтогов.Параметры.ИтогоПересортНедостачиКолво = ИтогоПересортНедостачиКолво;
		ОбластьИтогов.Параметры.ИтогоПересортНедостачиСумма = ИтогоПересортНедостачиСумма;
		ОбластьИтогов.Параметры.ИтогоПриходИзлишковКолво = ОбластьИтогов.Параметры.ИтогоРезультатИзлишекКолво - ОбластьИтогов.Параметры.ИтогоПересортИзлишкиКолво;
		ОбластьИтогов.Параметры.ИтогоПриходИзлишковСумма = ОбластьИтогов.Параметры.ИтогоРезультатИзлишекСумма - ОбластьИтогов.Параметры.ИтогоПересортИзлишкиСумма;
		ОбластьИтогов.Параметры.ИтогоСписаниеНедостачКолонка1Колво = ОбластьИтогов.Параметры.ИтогоРезультатНедостачаКолво - ОбластьИтогов.Параметры.ИтогоПересортНедостачиКолво;
		ОбластьИтогов.Параметры.ИтогоСписаниеНедостачКолонка1Сумма = ОбластьИтогов.Параметры.ИтогоРезультатНедостачаСумма - ОбластьИтогов.Параметры.ИтогоПересортНедостачиСумма;
		
		ТабДок.Вывести(ОбластьИтогов);
		//*Зернятко А.В.@06.11.2013 - //AKB-374. Ответственные лица СЕ
		//Руководители = обработкаПечатиСервер.ОтветственныеЛицаОрганизационнойЕдиницы(ВЫборка.Фирма,Док.Дата);
		Руководители = обработкаПечатиСервер.ОтветственныеЛицаОрганизационнойЕдиницы(ВЫборка.Фирма, Док.Дата, ВЫборка.СтруктурнаяЕдиница);
		///Зернятко А.В.@ - конец блока
		ОбластьМакета = Макет.ПолучитьОбласть("Подвал");
		
		ОбластьМакета.Параметры.ФИОБухгалтера =  Руководители.фиоглавногобухгалтера;
		ТабДок.Вывести(ОбластьМакета);
		
		// Зададим параметры макета
		ТабДок.ПолеСверху = 0;
		ТабДок.ПолеСлева  = 0;
		ТабДок.ПолеСнизу  = 0;
		ТабДок.ПолеСправа = 0;
		ТабДок.ОриентацияСтраницы = ОриентацияСтраницы.Ландшафт;
		УправлениеПечатью.ЗадатьОбластьПечатиДокумента(ТабДок, 
		НомерСтрокиНачало, ОбъектыПечати,Док.Ссылка);
		ТабДок.ВывестиГоризонтальныйРазделительСтраниц();	
		НомерСтрокиНачало = ТабДок.ВысотаТаблицы+1;
		КонецЦикла;
	конеццикла;
	Возврат ТабДок;
	
КонецФункции	


Функция ПечатьИНВ3(МассивОбъектов,ОбъектыПечати,флЗап) экспорт
	Валюта = " руб." ;
	ТабДок= новый ТабличныйДокумент;
	НомерСтрокиНачало = 0;
	для каждого Док из МассивОбъектов цикл
		ЗапросФИрмаСклад = новый Запрос;
		ЗапросФирмаСклад.Текст = "ВЫБРАТЬ РАЗЛИЧНЫЕ
		                         |	ИнвентаризацияСоставПоФирмам.Фирма,
		                         |	ИнвентаризацияСоставПоФирмам.Склад
		                         |ИЗ
		                         |	Документ.Инвентаризация.СоставПоФирмам КАК ИнвентаризацияСоставПоФирмам
		                         |ГДЕ
		                         |	ИнвентаризацияСоставПоФирмам.Ссылка = &Ссылка";
		ЗапросФирмаСклад.УстановитьПараметр("ссылка",док.ссылка);
		Рез = ЗапросФирмаСклад.Выполнить();
		ВыборкаФирмаСклад=Рез.Выбрать();
		Пока ВыборкаФирмаСклад.Следующий() цикл
		СтрФильтр =   " И Номенклатура в (ВЫБРАТЬ РАЗЛИЧНЫЕ
		|	ИнвентаризацияСостав.Номенклатура
		|ИЗ
		|	Документ.Инвентаризация.Состав КАК ИнвентаризацияСостав)";
		Текст = "ВЫБРАТЬ
		|	ЕСТЬNULL(Инвентаризация.Номенклатура, Было.Номенклатура) КАК Номенклатура,
		|	ЕСТЬNULL(Инвентаризация.Номенклатура.Код, Было.Номенклатура.Код) КАК ТоварКод,
		|	ЕСТЬNULL(Инвентаризация.Номенклатура.Наименование, Было.Номенклатура.Наименование) КАК НоменклатураНаименование,
		|	"+?(флЗап,"Инвентаризация.ЦенаИзлишка","0")+" КАК Цена,
		|	"+?(флЗап,"ЕстьNull(Было.КоличествоОстаток,0)","0")+" КАК КоличествоУчет,
		|"+?(флЗап,"ЕстьNull(Было.СуммаОстаток,0)","0")+" КАК СуммаУчет,
		|	"+?(флЗап,"ЕстьNull(Инвентаризация.Количество,0)","0")+" КАК КоличествоФакт,
		|	"+?(флЗап,"ЕстьNull(Стало.СуммаОстаток,0)","0")+" КАК СуммаФакт
		|ИЗ
		|	(ВЫБРАТЬ
		|		ИнвентаризацияСостав.Номенклатура КАК Номенклатура,
		|		ИнвентаризацияСостав.ЦенаИзлишка КАК ЦенаИзлишка,
		|		ИнвентаризацияСостав.Количество КАК Количество
		|	ИЗ
		|		Документ.Инвентаризация.СоставПоФирмам КАК ИнвентаризацияСостав
		|	ГДЕ
		|		ИнвентаризацияСостав.Ссылка = &Ссылка и ИнвентаризацияСостав.Фирма =&Фирма и ИнвентаризацияСостав.Склад =&Склад) КАК Инвентаризация
		|		ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ
		|			ОстаткиНоменклатурыОстатки.Номенклатура КАК Номенклатура,
		|			ОстаткиНоменклатурыОстатки.КоличествоОстаток КАК КоличествоОстаток,
		|			ОстаткиНоменклатурыОстатки.СуммаОстаток - ОстаткиНоменклатурыОстатки.СуммаНДСОстаток КАК СуммаОстаток
		|		ИЗ
		|			РегистрНакопления.ОстаткиНоменклатуры.Остатки(
		|					&Граница1,
		|					МестоХранения = &Склад
		|						И Фирма = &Фирма"+?(Док.ТолькоТаблица,СтрФильтр,"")+ ") КАК ОстаткиНоменклатурыОстатки) КАК Было
		|		ПО Инвентаризация.Номенклатура = Было.Номенклатура
		|		ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ
		|			ОстаткиНоменклатурыОстатки.Номенклатура КАК Номенклатура,
		|			ОстаткиНоменклатурыОстатки.КоличествоОстаток КАК КоличествоОстаток,
		|			ОстаткиНоменклатурыОстатки.СуммаОстаток - ОстаткиНоменклатурыОстатки.СуммаНДСОстаток КАК СуммаОстаток
		|		ИЗ
		|			РегистрНакопления.ОстаткиНоменклатуры.Остатки(
		|					&Граница2,
		|					МестоХранения = &Склад
		|						И Фирма = &Фирма"+СтрФильтр+") КАК ОстаткиНоменклатурыОстатки) КАК Стало
		|		ПО Инвентаризация.Номенклатура = Стало.Номенклатура
		|		ИТОГИ
		|	СУММА(КоличествоУчет),
		|	СУММА(СуммаУчет),
		|	СУММА(КоличествоФакт),
		|	СУММА(СуммаФакт)
		|ПО
		|	ОБЩИЕ ";
		Запрос = Новый Запрос(Текст);
		Запрос.УстановитьПараметр("Ссылка", Док.Ссылка);
		Запрос.УстановитьПараметр("Фирма", ВыборкаФирмаСклад.Фирма);
		Запрос.УстановитьПараметр("Склад", ВыборкаФирмаСклад.Склад);
		Запрос.УстановитьПараметр("Граница1", Новый Граница(Док.Ссылка.МоментВремени(), ВидГраницы.Исключая));
		Запрос.УстановитьПараметр("Граница2", Новый Граница(Док.Ссылка.МоментВремени(), ВидГраницы.Включая));
		ТабДок.ИмяПараметровПечати = "ПАРАМЕТРЫ_ПЕЧАТИ_Инвентаризация_ИНВ3";
		
		Макет = ПолучитьОбщийМакет("ИНВ3");
		// Выводим шапку накладной
		ОбластьМакета = Макет.ПолучитьОбласть("Шапка");
		
		СведенияОбОрганизации = ОбработкаПечатиСервер.СведенияОЮрФизЛице(ВыборкаФирмасклад.Фирма, Док.Дата, Док.СтруктурнаяЕдиница);
		ОбластьМакета.Параметры.ПредставлениеОрганизации = Обработкапечатисервер.ОписаниеОрганизации(СведенияОбОрганизации);
		ОбластьМакета.Параметры.Подразделение =Док.СтруктурнаяЕдиница.Наименование;
		ОбластьМакета.Параметры.Основание =  Док.Основание;
		ОбластьМакета.Параметры.ОрганизацияПоОКПО = СведенияОбОрганизации.КодПоОКПО;
		ОбластьМакета.Параметры.ДатаДокумента = Док.Дата;
		ОбластьМакета.Параметры.ДатаСнятияОстатков =Док.Дата;
		ОбластьМакета.Параметры.НомерДокумента = Док.Номер;
		ТабДок.Вывести(ОбластьМакета);
		ТабДок.ВывестиГоризонтальныйРазделительСтраниц();
		
		СтрокНаСтранице = 19;
		НомерСтраницы   = 2;
		Ном             = 0;  
		КолвоСтрокПоСтранице = 0;
		
		// Выводим заголовок таблицы
		ЗаголовокТаблицы = Макет.ПолучитьОбласть("ЗаголовокТаблицы");
		ЗаголовокТаблицы.Параметры.НомерСтраницы = "Страница " + НомерСтраницы; 
		ТабДок.Вывести(ЗаголовокТаблицы);
		
		// Выводим многострочную часть докмента
		ВыборкаСтрокОбщийИтог = Запрос.Выполнить().Выбрать(ОбходРезультатаЗапроса.ПоГруппировкам);//Шапка.Состав.Выбрать();
		ОбластьМакета = Макет.ПолучитьОбласть("Строка");
		ИтогоФактКоличество = 0;
		ИтогоФактКоличествоСтр = 0;
		ИтогоФактСуммаСтр = 0;
		ИтогоБухКоличествоСтр = 0;
		ИтогоБухСуммаСтр = 0;
		Пока ВыборкаСтрокОбщийИтог.Следующий() цикл
			ВыборкаСтрокТовары = ВыборкаСтрокОбщийИтог.Выбрать();
			Пока ВыборкаСтрокТовары.Следующий() Цикл
				Если НЕ ЗначениеЗаполнено(ВыборкаСтрокТовары.Номенклатура) Тогда
					Сообщить("В одной из строк не заполнено значение номенклатуры - строка при печати пропущена.", СтатусСообщения.Важное);
					Продолжить;
				КонецЕсли;        
				Ном = Ном + 1;
				Если КолвоСтрокПоСтранице = СтрокНаСтранице Тогда
					
					ОбластьИтоговПоСтранице = Макет.ПолучитьОбласть("ПодвалСтраницы");
					
					ОбластьИтоговПоСтранице.Параметры.КоличествоПорядковыхНомеровНаСтраницеПрописью     =ОбработкаПечатисервер.КоличествоПрописью(КолвоСтрокПоСтранице);
					ОбластьИтоговПоСтранице.Параметры.ИтогоФактКоличество = ИтогоФактКоличествоСтр; 
					
					
					ОбластьИтоговПоСтранице.Параметры.ИтогоФактСумма = ИтогоФактСуммаСтр;
					ОбластьИтоговПоСтранице.Параметры.ИтогоБухСумма = ИтогоБухСуммаСтр;
					
					
					ОбластьИтоговПоСтранице.Параметры.ИтогоБухКоличество = ИтогоБухКоличествоСтр;
					ОбластьИтоговПоСтранице.Параметры.ОбщееКоличествоЕдиницФактическиНаСтраницеПрописью =ОбработкаПечатисервер.КоличествоПрописью(ИтогоФАктКоличествоСтр);
					ОбластьИтоговПоСтранице.Параметры.СуммаФактическиНаСтраницеПрописью =ОбработкаПечатисервер.КоличествоПрописью(ИтогоФАктсуммаСтр);
					
					ТабДок.Вывести(ОбластьИтоговПоСтранице);
					КолвоСтрокПоСтранице = 0;
					НомерСтраницы = НомерСтраницы + 1;
					ТабДок.ВывестиГоризонтальныйРазделительСтраниц();
					Если Ном<>Док.состав.Количество() тогда
						ИтогоФактКоличествоСтр = 0;
						ИтогоФактСуммаСтр = 0;
						ИтогоБухКоличествоСтр = 0;
						ИтогоБухСуммаСтр = 0;
					Конецесли;
					ЗаголовокТаблицы.Параметры.НомерСтраницы = "Страница " + НомерСтраницы;
					ТабДок.Вывести(ЗаголовокТаблицы);
				КонецЕсли;
				
				ЕдиницаИзмерения = ВыборкаСтрокТовары.Номенклатура.БазоваяЕдиницаИзмерения;
				ОбластьМакета.Параметры.Заполнить(ВыборкаСтрокТовары);
				СуммаФАкт = ВыборкаСтрокТовары.СуммаФакт;				
				СуммаБух = ВыборкаСтрокТовары.СуммаУчет;
				
				ОбластьМакета.Параметры.ТоварКод =  ВыборкаСтрокТовары.Номенклатура.код;
				ОбластьМакета.Параметры.ТоварНаименование = ВыборкаСтрокТовары.НоменклатураНаименование;
				ОбластьМакета.Параметры.Номер = Ном;
				ОбластьМакета.Параметры.ЕдиницаИзмеренияНаименование = ЕдиницаИзмерения.Наименование;
				//ОбластьМакета.Параметры.ЕдиницаИзмеренияКодПоОКЕИ = ЕдиницаИзмерения.Код;
				
				КоличествоФакт = ВыборкаСтрокТовары.КоличествоФакт;
				КоличествоБух = ВыборкаСтрокТовары.КоличествоУчет;
				
				Если СуммаФАкт > 0 И КоличествоФакт > 0 Тогда
					
					ОбластьМакета.Параметры.Цена = Окр(СуммаФАкт / КоличествоФакт,2);
					
				ИначеЕсли СуммаБух > 0 И КоличествоБух > 0 Тогда
					
					ОбластьМакета.Параметры.Цена = Окр(СуммаБух  / КоличествоБух,2);
					
				КонецЕсли;	
				ОбластьМакета.Параметры.ФактКоличество = КоличествоФакт;
				ОбластьМакета.Параметры.ФактСумма = СуммаФакт;
				ОбластьМакета.Параметры.БухКоличество = КоличествоБух;
				ОбластьМакета.Параметры.БухСумма = СуммаБух;
				ИтогоФактКоличествоСтр = итогоФактКоличествоСтр + КоличествоФАкт;
				ИтогоФактСуммаСтр = итогоФАктСуммаСтр + СуммаФакт;
				ИтогоБухКОличествоСтр = итогобухКоличествоСтр +КоличествоБух;
				ИтогоБухСуммаСтр = ИтогоБухСуммаСтр + СуммаБух;
				ТабДок.Вывести(ОбластьМакета);
				КолвоСтрокПоСтранице = КолвоСтрокПоСтранице + 1;
			КонецЦикла;
			ОбластьИтоговПоСтранице = Макет.ПолучитьОбласть("ПодвалСтраницы");
			
			ОбластьИтоговПоСтранице.Параметры.КоличествоПорядковыхНомеровНаСтраницеПрописью = ОбработкаПечатисервер.КоличествоПрописью(КолвоСтрокПоСтранице);
			ОбластьИтоговПоСтранице.Параметры.ИтогоФактКоличество = ИтогоФактКоличествоСтр; 
			ОбластьИтоговПоСтранице.Параметры.ИтогоФактСумма = ИтогоФактСуммаСтр;
			ОбластьИтоговПоСтранице.Параметры.ИтогоБухКоличество = ИтогоБухКоличествоСтр;
			ОбластьИтоговПоСтранице.Параметры.ИтогоБухСумма = ИтогоБухСуммаСтр;
			ОбластьИтоговПоСтранице.Параметры.ОбщееКоличествоЕдиницФактическиНаСтраницеПрописью =ОбработкаПечатисервер.КоличествоПрописью(ИтогоФАктКоличествоСтр);
			ОбластьИтоговПоСтранице.Параметры.СуммаФактическиНаСтраницеПрописью =ОбработкаПечатисервер.ФорматироватьСуммуПлатежногоДокумента(ИтогоФАктсуммаСтр)+Валюта;
			
			ТабДок.Вывести(ОбластьИтоговПоСтранице);
			ОбластьИтоговПоСтранице = Макет.ПолучитьОбласть("ПодвалОписи");
			ОбластьИтоговПоСтранице.Параметры.ОбщееКоличествоЕдиницФактическиНаСтраницеПрописью = ОбработкаПечатисервер.КоличествоПрописью(выборкаСтрокОбщийИтог.КоличествоФакт); 
			ОбластьИтоговПоСтранице.Параметры.КоличествоПорядковыхНомеровНаСтраницеПрописью =ОбработкаПечатисервер.КоличествоПрописью(ВыборкаСтрокТовары.Количество());
			ОбластьИтоговПоСтранице.Параметры.ОбщееКоличествоЕдиницФактическиНаСтраницеПрописью = ОбработкаПечатисервер.КоличествоПрописью(ВыборкаСтрокОбщийИтог.КоличествоФакт);
			чВыборкаСтрокОбщийИтогСУммаФакт = ВыборкаСтрокОбщийИтог.СУммаФакт;
			ОбластьИтоговПоСтранице.Параметры.СуммаФактическиНаСтраницеПрописью =ОбработкаПечатисервер.ФорматироватьСуммуПлатежногоДокумента(чВыборкаСтрокОбщийИтогСУммаФакт)+валюта;
			ТабДок.Вывести(ОбластьИтоговПоСтранице);
		Конеццикла;
		
		// Выводим итоги по последней странице
		УправлениеПечатью.ЗадатьОбластьПечатиДокумента(ТабДок, 
		НомерСтрокиНачало, ОбъектыПечати,Док.Ссылка);
		ТабДок.ВывестиГоризонтальныйРазделительСтраниц();
		НомерСтрокиНачало = ТабДок.ВысотаТаблицы+1;
		Конеццикла;
	конеццикла;
	// Зададим параметры макета
	ТабДок.ПолеСверху = 0;
	ТабДок.ПолеСлева  = 0;
	ТабДок.ПолеСнизу  = 0;
	ТабДок.ПолеСправа = 0;
	ТабДок.ОриентацияСтраницы = ОриентацияСтраницы.Ландшафт;
	ТабДок.ВывестиГоризонтальныйРазделительСтраниц();
	
	Возврат ТабДок;
КонецФУнкции


Процедура Печать(МассивОбъектов, ПараметрыПечати, КоллекцияПечатныхФорм,
	ОбъектыПечати, ПараметрыВывода) Экспорт
	ПараметрыВывода.ДоступнаПечатьПоКомплектно = Истина;
	Если УправлениеПечатью.НужноПечататьМакет(КоллекцияПечатныхФорм, "ИНВ3") Тогда
		УправлениеПечатью.ВывестиТабличныйДокументВКоллекцию(КоллекцияПечатныхФорм,
		"ИНВ3", "ИНВ-3",ПечатьИНВ3(массивОбъектов,ОбъектыПечати,Ложь));
	ИначеЕсли УправлениеПечатью.НужноПечататьМакет(КоллекцияПечатныхФорм, "ИНВ3Заполненная") Тогда
		УправлениеПечатью.ВывестиТабличныйДокументВКоллекцию(КоллекцияПечатныхФорм,
		"ИНВ3Заполненная", "ИНВ-3 (заполненная)",ПечатьИНВ3(массивОбъектов,ОбъектыПечати,Истина));
	ИначеЕсли УправлениеПечатью.НужноПечататьМакет(КоллекцияПечатныхФорм, "ИНВ19") Тогда
		УправлениеПечатью.ВывестиТабличныйДокументВКоллекцию(КоллекцияПечатныхФорм,
		"ИНВ19", "ИНВ-19",ПечатьИНВ19(массивОбъектов,ОбъектыПечати));
	КонецЕсли;
КонецПроцедуры


