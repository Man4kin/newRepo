
&НаСервере
Процедура ЗаписатьНаСервере()
	тзДанные = ДанныеФормыВЗначение(тзГруппы,Тип("ТаблицаЗначений"));
	ПоместитьВоВременноеХранилище(тзДанные,Параметры.АдресХранилища);
	
КонецПроцедуры

&НаКлиенте
Процедура Записать(Команда)
	ЗаписатьНаСервере();
	ЭтаФорма.Закрыть(Истина);
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	тзДанные = ПолучитьИзВременногоХранилища(Параметры.АдресХранилища);
	ЗначениеВДанныеФормы(тзДанные,тзГруппы);
КонецПроцедуры
