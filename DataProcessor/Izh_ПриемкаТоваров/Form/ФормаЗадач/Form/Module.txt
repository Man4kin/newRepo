
&НаКлиенте
Процедура Далее(Команда)
	Закрыть(КодВозвратаДиалога.Да);
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Для Каждого Строка Из Параметры.Задачи Цикл
		НоваяСтрока = Объект.Задачи.Добавить();
		НоваяСтрока.Задача = Строка.Задача;
		НоваяСтрока.Дата = Строка.Дата;
		НоваяСтрока.Номер = Строка.Номер;
	КонецЦикла;
КонецПроцедуры


&НаКлиенте
Процедура ЗадачиВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	Д = Новый Структура("Ключ", Элементы.Задачи.ТекущиеДанные.Задача);
	Форма = ПолучитьФорму("Задача.ЗадачаИсполнителя.ФормаОбъекта", Д);
	Форма.Открыть();
    Закрыть(КодВозвратаДиалога.Нет);
КонецПроцедуры

