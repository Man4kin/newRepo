
&НаСервере
Процедура ВыгрузитьНаСервере()
	
	Если БезОстатка И (НЕ ЗначениеЗаполнено(Номенклатура) ИЛИ Номенклатура.Группа = 1) Тогда  //dps+ 15,09,22 Исключение выгрузки в ПОС всей номенклатуры буз остатка
		Сообщение = Новый СообщениеПользователю;
		Сообщение.Текст = "При выгрузке без остатка необходимо выбрать номенклатуру (не группу)!";
		Сообщение.Поле = "Номенклатура";
		Сообщение.Сообщить();
		Возврат;
	КонецЕсли;
	
	Izh_ОбменДаннымиPOS.Выгрузить(Номенклатура,БезОстатка);
КонецПроцедуры

&НаКлиенте
Процедура Выгрузить(Команда)
	ВыгрузитьНаСервере();
КонецПроцедуры
