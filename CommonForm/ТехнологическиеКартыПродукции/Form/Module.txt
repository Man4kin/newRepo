
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Продукция = Неопределено ИЛИ Параметры.Продукция.Пустая() Тогда
		Отказ = Истина;
		Возврат;
	КонецЕсли;
	
	Продукция = Параметры.Продукция; 
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	ТехнологическиеКарты.Параметры.УстановитьЗначениеПараметра("Продукция", Продукция);

КонецПроцедуры

&НаКлиенте
Процедура ТехнологическиеКартыПередНачаломИзменения(Элемент, Отказ)
	СтруктураПараметров = Новый Структура("Ключ", Элементы.ТехнологическиеКарты.ТекущиеДанные.ТехнологическаяКарта);
	ОткрытьФорму("Документ.ТехнологическаяКарта.Форма.ФормаДокумента",СтруктураПараметров,ЭтаФорма);
	Отказ = Истина;
КонецПроцедуры
