
Функция КолДвиж(НаборДвижений) Экспорт 
	
	// !!!! efim
	#Если _ Тогда
		НаборДвижений = РегистрыНакопления.ДвиженияТМЦ.СоздатьНаборЗаписей();
	#КонецЕсли
	// !!!!
	
	УстановитьПривилегированныйРежим(Истина);
	
	НаборЗаписейМетаданные = НаборДвижений.Метаданные();
	
	Если ОбщегоНазначения.ЭтоРегистрСведений(НаборЗаписейМетаданные) Тогда
		Менеджер = РегистрыСведений;
	ИначеЕсли ОбщегоНазначения.ЭтоРегистрНакопления(НаборЗаписейМетаданные) Тогда
		Менеджер = РегистрыНакопления;
	КонецЕсли;
	
	НаборЗаписей = Менеджер[НаборЗаписейМетаданные.Имя].СоздатьНаборЗаписей();
	// !!!! efim
	#Если _ Тогда
		НаборЗаписей = РегистрыНакопления.ДвиженияТМЦ.СоздатьНаборЗаписей();
	#КонецЕсли
	// !!!!
	НаборЗаписей.Отбор.Регистратор.Установить(НаборДвижений.Отбор.Регистратор.Значение);
	
	НаборЗаписей.Прочитать();
	
	Возврат НаборЗаписей.Количество();
	
КонецФункции

Функция ГетВТ(МенеджерВТ, Знач Таблица) Экспорт 
	
	Возврат Izh_ОбщегоНазначения.ПолучитьВТ(МенеджерВТ, Таблица);
	
КонецФункции
