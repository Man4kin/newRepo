
Процедура УстановитьВидимостьЭлементов(Форма, Знач МассивЭлементов, Знач Видимость) Экспорт 
	
	УстановитьСвойствоЭлементов(Форма, МассивЭлементов, "Видимость", Видимость);
	
КонецПроцедуры

Процедура УстановитьДоступностьЭлементов(Форма, Знач МассивЭлементов, Знач Доступность) Экспорт 
	
	Если ТипЗнч(МассивЭлементов) = Тип("Строка") Тогда
		МассивЭлементов = Izh_СтроковыеФункцииКлиентСервер.РазложитьСтрокуВМассивПодстрок(МассивЭлементов,, Истина, Истина);
	КонецЕсли;
	
	Для каждого Элемент Из МассивЭлементов Цикл
		Попытка
			ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Форма.Элементы, Элемент, "ТолькоПросмотр", НЕ Доступность);
		Исключение
			ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Форма.Элементы, Элемент, "Доступность", Доступность);
		КонецПопытки;
	КонецЦикла;
	
КонецПроцедуры

Процедура УстановитьСвойствоЭлементов(Форма, Знач МассивЭлементов, Знач ИмяСвойства, Знач Значение) Экспорт 
	
	Если ТипЗнч(МассивЭлементов) = Тип("Строка") Тогда
		МассивЭлементов = Izh_СтроковыеФункцииКлиентСервер.РазложитьСтрокуВМассивПодстрок(МассивЭлементов, ",", Истина, Истина);
	КонецЕсли;
	
	Для каждого Элемент Из МассивЭлементов Цикл
		ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Форма.Элементы, Элемент, ИмяСвойства, Значение);
	КонецЦикла;
	
КонецПроцедуры
